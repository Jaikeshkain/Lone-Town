import React from 'react';

interface MatchCardProps {
  // Add props as needed
}

const MatchCard: React.FC<MatchCardProps> = () => {
  return (
    <div className="match-card">
      <h2>Daily Match</h2>
      {/* Add match card content */}
    </div>
  );
};

export default MatchCard; 